## 1. Package Domain and Manifest v1 Clean Cutover

- [x] 1.1 Add immutable host-domain configuration data/UI declaration types, the three stable public capability identifiers (`audio.transcription`, `audio.synthesis`, and `audio.playback`), and finite validation-policy values in `app/src/main/java/dev/nilp0inter/subspace/dependency/PackageDomain.kt`; cover constructor invariants and defensive immutability in `PackageValidatorContractTest.kt`.
- [x] 1.2 Extend `PackageManifest` and `ValidatedPackageRevision` in `PackageDomain.kt` so every accepted manifest-v1 revision retains mandatory `configuration` and `capabilities` declarations alongside the unchanged `manifestVersion = 1`, `Lua 5.4`, and `subspace-lua-v1` contract; prove omission cannot be represented as an inferred empty declaration.
- [x] 1.3 Extend the strict duplicate-key decoder in `app/src/main/java/dev/nilp0inter/subspace/dependency/PackageValidator.kt` to require exactly the evolved root, configuration, data, UI, field, and choice key sets and reject missing, unknown, mistyped, null, or duplicate members before program-image creation or storage.
- [x] 1.4 Implement static data-schema validation in `PackageValidator.kt` for at most 32 unique `[a-z][a-z0-9_]*` required scalar fields: exact-type defaults, optional nonempty unique string `allowedValues`, Boolean fields without type-specific keys, and signed-64-bit integer ranges/defaults; add focused acceptance and rejection cases to `PackageValidatorContractTest.kt`.
- [x] 1.5 Implement static UI-schema validation in `PackageValidator.kt` so every data field appears exactly once in declared render order with a type-compatible `text`, `toggle`, `number`, or `choice` control, exact choice-value set equality, unique values and labels, and no `choices` on non-choice controls; test omitted, duplicate, dangling, extra, and mismatched entries.
- [x] 1.6 Enforce the configuration limits from the contract—64 UTF-8 bytes per field ID, 128 per label, 512 per help string, 64 values per choice, 16 KiB per string value, and 64 KiB per canonical payload—without replacing the existing archive, manifest, source-map, or expansion limits; verify every boundary and first-over-limit rejection without Lua execution.
- [x] 1.7 Validate `capabilities` as an ordered duplicate-free array containing only the three stable semantic IDs, compile neither Kotlin capability keys nor host classes during parsing, and return typed capability-validation failures before provider registration or capability acquisition.
- [x] 1.8 Thread the evolved manifest through `InstalledPackageStore.kt` and `InstalledPackageRepository.kt` so exact stored bytes are rehashed and fully reparsed into checked declarations on load/materialization rather than trusting a cached model; extend `InstalledPackageTransactionsTest.kt` for round-trip, corruption isolation, commit failure, and no-Lua/no-registry side effects.
- [x] 1.9 Cut over every in-tree package builder and immutable fixture, including `app/src/test/resources/diagnostics-channel/subspace-channel.zip`, to explicit declarations in the same change as the parser; retain byte-pinned historical Diagnostics v1.0.0/v1.1.0 inputs only as rejection fixtures and prove no legacy parser, omitted-field default, v2 dispatcher, callback shim, or compatibility negotiation path exists.

## 2. Installed Provider Configuration and Generic Host Editing

- [x] 2.1 Replace `LuaEmptyConfigurationProvider` in `LuaChannelImplementationProvider.kt` with a package-specific schema-version-1 provider compiled by `LuaPackageMaterializer.kt` from the validated declaration, producing the complete default `OpaqueJsonObject` without loading a Lua module or creating an actor/state.
- [x] 2.2 Compile declared UI entries into the existing `ChannelConfigurationField` algebra in `ChannelImplementationProvider.kt`, extending that algebra only where required for bounded integer editing; preserve declared order, labels/help, exact choice values, and package-independent host rendering.
- [x] 2.3 Implement exact payload validation in the compiled provider: every declared field present, no undeclared fields, exact JSON scalar types, no float/null/nested/array values, no coercion or default filling, allowed-value/range enforcement, per-value bounds, and canonical 64-KiB total bound; test each failure as a typed provider configuration error.
- [x] 2.4 Materialize installed descriptors whose implementation ID, configuration-provider ID, installed-snapshot key, repository-derived ID, artifact-digest fingerprint, fields, defaults, and compiled capability eligibility agree; extend `InstalledProviderRegistryContractTest.kt` to prove publication creates no Lua actor/state and rejects inconsistent snapshots atomically.
- [x] 2.5 Route installed-provider creation and editing exclusively through `ChannelConfigurationScreen.kt`, `ServiceChannelManager.kt`, `ChannelRepository.kt`, and `ChannelCatalogueCodec.kt`; verify text, toggle, number, and choice controls render in declaration order and an empty schema renders an empty edit card without a package-specific screen.
- [x] 2.6 Preserve accepted configuration payloads losslessly per catalogue instance across create, edit, reorder, rename, active-selection changes, and process restart; extend `ChannelConfigurationPayloadTest.kt`, `ChannelCatalogueTest.kt`, and `ServiceChannelManagerTest.kt` to prove two same-provider instances retain isolated payloads and sibling state.
- [x] 2.7 Make package publication/update revalidate every preserved instance payload against the candidate declaration before runtime construction; on incompatibility preserve the exact payload and definition, project a typed configuration-unavailable result, create no successor/default substitute, and prove explicit rollback restores availability.
- [x] 2.8 Keep a configuration commit and runtime replacement as one atomic generic cutover in `ServiceChannelManager.kt` and `ChannelRuntimeRegistry.kt`: reject invalid edits without changing catalogue/runtime, and for valid edits stop predecessor admission, finish a committed terminal callback, drain/cancel descendants, revoke leases and queued playback, close the predecessor, then start a fresh generation with the new payload.
- [x] 2.9 Add focused generic configuration tests in `ChannelConfigurationUiHelpersTest.kt`, `ChannelConfigurationPayloadTest.kt`, `ChannelCatalogueTest.kt`, `InstalledProviderRuntimeReconciliationTest.kt`, and `ChannelRuntimeRegistryTest.kt` for defaults, render ordering, malformed persisted data, lossless restart, sibling isolation, incompatible update/rollback, and drain-before-successor-ready replacement.

## 3. Lua Callback, Readiness, and Execution-Context Cutover

- [x] 3.1 Evolve the Kotlin/native callback bridge atomically across `LuaKernelBridge.kt`, `LuaNativeKernel.kt`, `LuaNativeKernelBridge.kt`, `rust/subspace-lua-actor/subspace-lua-actor/src/jni_bridge.rs`, and `state.rs` so the mandatory startup invocation carries one normalized configuration argument; remove the no-argument invocation path rather than retaining an overload or retry.
- [x] 3.2 Convert each validated `ValidatedChannelConfiguration` into a fresh Lua table `{schema_version = 1, values = {...}}` during runtime generation construction in `LuaChannelImplementationProvider.kt`/`LuaAdapterRuntime.kt`; prove Lua mutation cannot affect the host payload, a sibling instance, or a later generation.
- [x] 3.3 Keep callback-table construction fail closed: `startup` is required and functional, recognized optional callbacks must be nil/absent or functions, a metatable/non-table/wrong type fails construction, unknown callback keys remain ignored, and no partial callback table or readiness state survives failure.
- [x] 3.4 Preserve serialized activation ordering in `LuaAdapterRuntime.kt`: invoke `startup(configuration)` exactly once, then optional lifecycle-ready, publish Ready only after both succeed, and run startup-admitted tasks only after publication; discard every admitted task on either activation failure.
- [x] 3.5 Build `handle_readiness` context from exactly the package-declared public capability IDs with values `available`, `recoverable`, or `unavailable`, validate the exact `{ready, status?}` result, project bounded `status` into `ChannelRuntimeSnapshot.summary`, and cache not-ready plus one bounded diagnostic for malformed results.
- [x] 3.6 Convert `handle_input` to a host-managed yield-capable coroutine while leaving startup, lifecycle, readiness, and SOS synchronous; release the serialized actor slot while an authorized semantic audio operation is pending, resume only its current execution owner, and validate only exact `{ok = true}` or `{error = {code, detail}}` terminal results.
- [x] 3.7 Preserve the module-load effect guard and context precedence: any host call during entry or lazy-module evaluation fails and discards the complete load, while post-load calls from an ineligible callback or unmanaged child coroutine return `E_INVALID_CONTEXT` before suspension/effect if Lua handles the pair.
- [x] 3.8 Preserve existing `subspace.runtime` behavior while extending input: spawn only from startup or managed tasks, sleep only from managed tasks, finite argument/task/timer bounds, pure-Lua child yields only, `E_INVALID_YIELD` for a raw boundary escape, operation-specific sleep deadlines, no generic task wall-clock expiry, live cancellation delivery, and no resume after generation close; cover these regressions in `runtime_v1_conformance.rs`, `LuaAdapterRuntimeTest.kt`, and `ActorRuntimeContractTest.kt`.
- [x] 3.9 Extend immutable black-box fixtures in `LuaProviderRegistryIntegrationTest.kt` to verify detached empty/configured startup snapshots, package-local `require`, proactive unselected timers, normalized `(nil, error_table)` results, two configured same-provider instances, compatibility and source-bound rejection before state creation, and fresh-generation configuration replacement through the real provider/runtime registries.

## 4. Opaque Audio Userdata, Ownership, and Resource Quotas

- [x] 4.1 Add a host-owned opaque-audio registry scoped to one Lua state and runtime generation, keyed by unguessable state-local tokens and explicit input-invocation or managed-task execution owners; registry entries retain only host `OpaqueAudioRecording`/`OpaqueSynthesizedAudio` artifacts and ownership metadata, never routes or platform pointers.
- [x] 4.2 Add native full-userdata support in `state.rs` with a locked metatable, no public fields/methods, and no real-metatable access; verify Lua cannot inspect, write, call, forge, stringify into a usable token, or recover PCM/path/device/route/JVM/native identities.
- [x] 4.3 Extend the JNI/Kotlin bridge as one atomic boundary so host code can inject and resolve opaque audio without serializing its token through JSON or exposing registry indexes, capability leases, actor-operation tokens, coroutine handles, or Kotlin objects.
- [x] 4.4 Deliver one captured-audio userdata in the bounded `handle_input` capture event only after valid terminal capture and registry admission; preserve the existing no-callback paths for capture cancellation/failure and dispose the recording if userdata admission cannot complete.
- [x] 4.5 Update every normalization, callback-result, structured-error, configuration, and `subspace.log` path to reject a value containing audio userdata with `E_INVALID_VALUE` atomically and without partial serialization, persistence, or logging; retain userdata only as a direct argument/result of its specified semantic audio functions.
- [x] 4.6 Reject wrong-kind, foreign-state, sibling-instance, predecessor-generation, different-callback, and different-task userdata before capability acquisition or host effect with the specified `E_INVALID_ARGUMENT` foreign-handle result; verify no ownership probe mutates the valid owner’s entry.
- [x] 4.7 Implement borrow/consume semantics: transcription borrows live captured audio, successful deferred-queue admission atomically consumes captured or synthesized audio, subsequent use returns `E_STALE`, and failed admission leaves the handle usable until its owner terminates.
- [x] 4.8 Dispose and invalidate every unconsumed artifact when its input callback or managed task terminates; on generation replacement/close invalidate all mappings, dispose all unscheduled artifacts, return `E_CLOSED` for attempted post-close use, and make Lua garbage collection incapable of releasing routes or initiating/cancelling effects.
- [x] 4.9 Enforce finite positive per-artifact retained-byte and duration limits plus live-token and retained-byte limits per execution owner, runtime generation, and process; add focused native/JVM tests proving first-over-limit capture and synthesis artifacts are disposed before userdata delivery, quota failures create no token, oversize artifacts are neither truncated nor partially registered, sibling quotas remain intact, and close returns all accounting to zero.

## 5. Public Semantic Audio Modules and Exactly-Once Operations

- [x] 5.1 Reserve and inject exactly the three additional modules `subspace.transcription`, `subspace.synthesis`, and `subspace.playback` beside the existing runtime/channel/log modules for every package; reject package source that shadows them and prove `require` alone grants no capability.
- [x] 5.2 Validate each audio call in this order before suspension/effect: load/callback context, exact arguments, current execution owner, live userdata kind/ownership, manifest eligibility, live capability lease/availability, and resource bounds; test that every early rejection allocates no host operation.
- [x] 5.3 Implement `transcription.transcribe(captured_audio)` over the semantic transcription capability, borrowing the handle and returning only `({text = <bounded valid UTF-8>}, nil)` or a normalized error; reject synthesized/wrong-owner audio without invoking the backend.
- [x] 5.4 Implement `synthesis.synthesize(params)` with exact keys, bounded nonblank UTF-8 text, bounded BCP-47 language, bounded logical voice, optional finite positive speed defaulting to `1.0`, and success as execution-owned synthesized-audio userdata; reject invalid/missing/extra/non-finite values before allocation.
- [x] 5.5 Implement `playback.schedule(audio, options)` with an exact options table, finite nonnegative bounded `delay_seconds` defaulting to zero, and success only after one atomic selection-aware queue admission and handle consumption; return only `({status = "scheduled"}, nil)` and never expose a queue token, operation, route, or platform object.
- [x] 5.6 Centralize Lua-visible audio failures to exactly `E_INVALID_ARGUMENT`, `E_INVALID_VALUE`, `E_INVALID_CONTEXT`, `E_CAPABILITY_UNDECLARED`, `E_UNAVAILABLE`, `E_BUSY`, `E_TIMEOUT`, `E_CANCELLED`, `E_CLOSED`, `E_STALE`, and `E_HOST_FAILURE`, with only bounded language-neutral reasons and no exception, endpoint, path, credential, SDK, or transport detail.
- [x] 5.7 Bind every admitted semantic call to an actor operation token, execution owner, and generation capability identity and race success, host failure, deadline, live-input cancellation, task cancellation, and generation revocation through one idempotent terminal gate; duplicate/late completions must not resume Lua or commit a second effect.
- [x] 5.8 Apply finite host-configured deadlines from successful operation admission through terminal completion: timed-out transcription leaves its borrowed capture live, timed-out synthesis creates no audio token, and timed-out queue admission creates no entry and leaves input audio unconsumed; prove late backend completion is stale.
- [x] 5.9 Distinguish cancellation boundaries: a still-live cancelled input resumes exactly once with `E_CANCELLED` for bounded cleanup, a cancelled managed task discards its suspended coroutine and audio without re-entry, and generation close discards all suspended executions without delivering cancellation/closed results.
- [x] 5.10 Extend `runtime_v1_conformance.rs`, `LuaNativeKernelBridgeTest.kt`, `LuaAdapterRuntimeTest.kt`, `ActorRuntimeContractTest.kt`, and `LuaActorInstrumentationTest.kt` for exact module/function shapes, argument failures, valid yields releasing the slot, invalid contexts, load-guard precedence, deadlines, cancellation/close races, duplicate completion, and late-effect suppression.

## 6. Host Capability Adapters, Deferred Queue, and Route Ownership

- [x] 6.1 Compile public capability IDs in `LuaPackageMaterializer.kt` to the existing internal semantic requirements only: transcription lease, synthesis lease, and the audio-operation plus deferred-playback mechanisms behind public `audio.playback`; keep Kotlin `CapabilityKey` names and implementation classes outside package/runtime data.
- [x] 6.2 Derive readiness availability and call-time authorization from the same compiled allowlist and live `RevocableChannelCapabilityScope`; prove undeclared access returns `E_CAPABILITY_UNDECLARED` before host acquisition and declared-but-unavailable access returns the normalized availability outcome without mode fallback.
- [x] 6.3 Adapt `BuiltInCapabilityAdapters.kt`, `ServiceChannelCapabilityHost.kt`, and actor mediation so transcription, synthesis, and playback operate on host-owned opaque artifacts with generation identity and typed outcomes while capture devices, engines, PCM arrays, paths, Android resources, and routes remain host-internal.
- [x] 6.4 Extend `DeferredAudioPlaybackCoordinator` in `DelayedPlaybackCoordinator.kt` with finite entry-count and retained-audio-byte limits per instance, generation, and process; reject an over-capacity admission with typed busy before consuming the caller’s handle, creating a partial entry, evicting a sibling entry, or rerouting existing audio.
- [x] 6.5 Make the deferred queue channel-associated FIFO: enforce delayed eligibility without Lua sleep, prevent later entries overtaking the head, retain authorized entries while another channel is selected, never play them through that channel, and re-evaluate admission if the original channel is selected again.
- [x] 6.6 Attach instance/generation authorization to every queued entry and revoke it idempotently on disablement, package update/rollback/removal, configuration replacement, runtime close, or service shutdown; remove predecessor entries and suppress late queue/backend completions without affecting successor or sibling generations.
- [x] 6.7 Resolve a physical route only when playback is admitted through `HostAudioCoordinator` and `ModePlaybackRouteResolver`: Work reacquires target RSM Bluetooth SCO with voice-communication usage, On-a-pinch uses normal mode plus built-in speaker/media usage, and On-the-road waits for Telecom capture release before validated car-media output; never reuse the PTT route or choose ambient fallback.
- [x] 6.8 Add focused tests in `RevocableChannelCapabilityScopeTest.kt`, `ServiceChannelCapabilityHostTest.kt`, `DeferredAudioPlaybackCoordinatorTest.kt`, `HostAudioCoordinatorTest.kt`, and `PlaybackRouteStrategyTest.kt` for public-ID mapping, undeclared denial, queue count/byte bounds, exact-once admission, failed-admission non-consumption, FIFO delay, temporary selection, route failure cleanup, revocation, and stale completion.
- [x] 6.9 Verify existing built-in Kotlin providers continue to use the implementation-neutral capability ports without constructing a Lua state, and enumerate the Lua namespace to prove no HTTP, filesystem, socket, networking, event-loop, package-management, or persistent plugin-state module was introduced.

## 7. External Debug Package Before Publication

- [x] 7.1 Create `nilp0inter/debug-channel` under immutable GitHub owner database ID `1224006`, resolve and record its positive immutable repository database ID, and use that ID—not coordinates, login text, or an invented value—as the manifest/provider identity; do not publish a release yet.
- [x] 7.2 Author the Debug manifest with the unchanged exact v1 runtime values, schema-version-1 `mode` string defaulting to `ECHO`, the five exact allowed values and matching ordered choice labels, and all three public audio capability declarations; verify no unknown key or host-resource declaration is present.
- [x] 7.3 Implement `startup(configuration)` to validate/capture the detached mode, `handle_readiness(context)` to check only the selected mode’s dependency subset and return its exact mode token as status, and omit `handle_sos` so the generic neutral default remains authoritative.
- [x] 7.4 Implement `ECHO` with zero-delay captured-audio scheduling and `DELAYED_ECHO` with host-side `delay_seconds = 5.0` scheduling; prove the package never sleeps while retaining captured audio and two delayed inputs remain FIFO.
- [x] 7.5 Implement `STT` as transcription plus bounded transcript logging without playback, `TTS` using exact text `Debug synthesis test`, language `en`, voice `default`, and speed `1.0`, and `STT_TTS` as transcription then synthesis of the exact transcript then zero-delay scheduling; return exact terminal success/failure without fallback or mode substitution.
- [x] 7.6 Add deterministic source-only assembly in the companion repository that emits one `subspace-channel.zip` containing only `manifest.json` and canonical UTF-8 `lua/*.lua` modules, with no bytecode, native code, workflow artifact, branch archive, asset payload, signature, or unexpected file.
- [x] 7.7 Validate the exact locally assembled bytes with the application’s real `PackageValidator`, then install them through `InstalledPackageRepository` and materialize the repository-derived provider without Lua execution at inspection/registration; block publication if identity, digest, manifest, source-map, or no-state-at-registration checks fail.
- [x] 7.8 Add an immutable Debug package fixture and integration tests that create instances only through the generic catalogue/provider/runtime path and prove the default, all readiness dependency sets, five operation sequences/constants/delay, `IDLE -> RECORDING -> PROCESSING -> SUCCESS|FAILED`, cancellation to IDLE, multiple-instance isolation, mode edit replacement, and no direct capability-port acceptance shortcut.

## 8. Diagnostics v1.2.0 Clean-Cutover Package

- [x] 8.1 Update `nilp0inter/diagnostics-channel` source for v1.2.0 so its manifest explicitly contains schema-version-1 empty data/UI field arrays and an empty capabilities array while retaining its durable repository identity and exact Lua/API requirements.
- [x] 8.2 Change Diagnostics startup to accept and validate exactly `{schema_version = 1, values = {}}`, emit its structured startup diagnostic, and admit its generation-owned heartbeat with typed failure if task capacity or generation liveness prevents admission.
- [x] 8.3 Update Diagnostics `handle_input` for the audio-bearing capture event while never reading, retaining, serializing, logging, transcribing, synthesizing, or scheduling the userdata; log only bounded non-sensitive metadata and rely on callback termination to reclaim the recording.
- [x] 8.4 Build deterministic local v1.2.0 bytes containing only the evolved manifest and canonical Lua sources, validate them through the real application validator/materializer, and update the in-tree immutable fixture without bundling it into production resources.
- [x] 8.5 Add exact old-release rejection and explicit-update tests: v1.0.0/v1.1.0 fail before storage/registration because declarations are missing, while an explicit update to v1.2.0 validates and executes without a legacy parser, inferred empties, configuration migration, or callback fallback.
- [x] 8.6 Extend `ExternalDiagnosticsChannelContractTest.kt` and `ExternalDiagnosticsChannelRuntimeContractTest.kt` for empty startup snapshot, independent instances/state/timers/logs, heartbeat admission failure, readiness/lifecycle, unselected heartbeat, metadata-only audio disposal, malformed input, SOS, replacement, removal/reinstall, and fresh restart state through ordinary installed-provider paths.

## 9. Atomic Built-in Debug Removal and Legacy Catalogue Behavior

- [x] 9.1 After the exact local external Debug artifact passes group 7, remove `DebugBuiltInProvider`/`DebugRuntime` in `DebugRuntimeFactory.kt`, `DebugMode.kt`, Debug configuration codec/provider/descriptor and `BuiltInChannelImplementationIds.DEBUG` in `ChannelImplementationProvider.kt`, service registration in `PttForegroundService.kt`, and `DebugChannelWidgets.kt` as one clean cutover with no intermediate fallback or duplicate provider.
- [x] 9.2 Migrate every remaining production/test caller away from Debug-specific types and delete tests that defend the Kotlin runtime path, including Debug portions of `BuiltInRuntimesTest.kt`; replace required parity coverage with the immutable external-package tests rather than retaining aliases, re-exports, fixture providers, or shims.
- [x] 9.3 Change `ChannelRepository.kt` legacy preference seeding so a fresh catalogue contains the existing non-Debug providers in their prior order/IDs, never reads or seeds `debug_channel_mode`, preserves a valid non-Debug active selection, and selects an enabled seeded fallback when legacy active selection named Debug; test failed commit/retry and existing-catalogue no-merge behavior.
- [x] 9.4 Preserve already-persisted definitions whose literal provider reference is `builtin:debug` byte-for-byte through `ChannelCatalogueCodec.kt` and the ordinary missing-provider path—instance ID, name, position, enabled state, schema version, and opaque payload—while projecting unavailable and excluding them from active PTT execution.
- [x] 9.5 Prove installing the repository-derived external Debug provider does not rebind, alias, copy configuration from, reuse the ID of, select, mutate, or delete an existing `builtin:debug` definition; require manual generic creation of a new instance.
- [x] 9.6 Update provider-registry, catalogue, service-composition, and instrumentation tests so ordinary startup registers Journal, Keyboard, and OpenAI Agent as before, leaves `builtin:debug` unresolved, creates no Lua state without installed packages, and preserves unrelated valid definitions when legacy Debug is unavailable.
- [x] 9.7 Inspect debug and release-equivalent APK contents after the cutover and fail if they contain Debug package source/archive bytes, a built-in Debug class/descriptor/ID/widget, a test-only Debug provider, duplicate Lua bridge, loose source loader, or automatic Debug install/instance-creation path.

## 10. Layered Automated Vertical Verification

- [x] 10.1 Complete the manifest/declaration layer in `PackageValidatorContractTest.kt`: exact evolved keys/types, duplicate and unknown rejection at every nesting level, scalar/default/range/UI matching, all finite bounds, capability allowlist, valid empty Diagnostics declarations, old artifact rejection, invalid UTF-8/bytecode, adversarial non-execution, and no partial retention.
- [x] 10.2 Complete the store/materialization layer in `InstalledPackageTransactionsTest.kt`, `InstalledLuaPackagesSmokeTest.kt`, and `InstalledProviderRegistryContractTest.kt`: exact digest revalidation, evolved manifest round-trip, crash-safe commit/recovery, declaration compilation, fingerprint publication, no state at registration, incompatible payload isolation, and one malformed provider not blocking valid siblings.
- [x] 10.3 Complete the generic-configuration layer in the focused UI/catalogue/service/registry tests: default creation, all host controls, exact edit rejection, opaque persistence/restart, two-instance isolation, package-update incompatibility and rollback, and configuration-triggered predecessor drain/revocation before successor readiness.
- [x] 10.4 Complete the Lua/actor layer in Rust and Kotlin tests: detached snapshots, locked/nonserializable userdata, wrong-kind/foreign/stale/consumed/closed handling, per-artifact and registry quotas, exact module argument/result/error shapes, context eligibility, yielded-slot release, timeouts, live cancellation, task cancellation, generation close, duplicate terminal, and stale suppression.
- [x] 10.5 Complete the capability/playback layer through real revocable scopes and coordinators: semantic ID mapping, undeclared denial before effect, mode-dependent availability, transcription borrow, playback consume, failed-admission liveness, bounded queue admission, FIFO/selection retention, route/platform non-leakage, generation revocation, and exact-once physical-effect authorization.
- [x] 10.6 Add `ExternalDebugChannelContractTest.kt`/`ExternalDebugChannelRuntimeContractTest.kt` (or equivalently focused files) that start from the exact package archive and traverse validator -> installed store -> materializer -> provider registry -> catalogue -> runtime registry -> actor -> capability adapters for all five modes, without a test-only Debug provider or direct capability-port acceptance path.
- [x] 10.7 Exercise Debug install, generic instance creation, compatible update, incompatible update, explicit rollback, removal, reinstall, configuration replacement, disablement, and process restart with distinguishable exact revisions; assert predecessor close before successor startup, preserved definitions/payloads, removed queued effects, and no restored Lua globals/timers/coroutines/tokens.
- [x] 10.8 Exercise the exact Diagnostics v1.2.0 fixture through the same provider path and prove old-release failure, explicit update recovery, empty configuration/no capabilities, unchanged heartbeat/log/SOS behavior, opaque-audio disposal, update/rollback/removal/reinstall, and fresh restart state.
- [ ] 10.9 Extend `InstalledLuaPackagesInstrumentationTest.kt` and `LuaActorInstrumentationTest.kt` to run the packaged JNI and production installed-provider path for configured startup, audio userdata/modules, Debug mode execution, configuration/revision replacement, cancellation/revocation, sibling isolation, and no built-in Debug resolution; keep immutable fixtures test-only.

## 11. External Publication and Exact Provenance Gates

- [ ] 11.1 Gate publication on the exact local Diagnostics v1.2.0 and Debug v1.0.0 candidate archives passing their real validator, immutable-store/materializer smoke paths, all focused tests in groups 1–10, built-in Debug absence, and release-equivalent package-content inspection; do not use an external release to discover a failing contract.
- [x] 11.2 Publish Diagnostics first as immutable stable, non-draft, non-prerelease tag `v1.2.0` in `nilp0inter/diagnostics-channel` with exactly one uploaded canonical `subspace-channel.zip` asset; do not replace or mutate historical v1.0.0/v1.1.0 releases or their assets.
- [x] 11.3 Record Diagnostics v1.2.0 provenance from GitHub and the exact uploaded bytes: immutable owner ID, repository ID, release ID, asset ID, tag, publication timestamp, asset name/size, and SHA-256; label the digest only as exact-byte integrity and make no signature, attestation, review, audit, or defect-freedom claim.
- [x] 11.4 Re-resolve Diagnostics anonymously through the production GitHub inspection path, verify Official classification derives from owner database ID `1224006`, independently download the canonical asset, match the recorded digest/size/IDs, pass the evolved validator, and prove the historical assets still fail only for the mandatory-manifest clean cutover.
- [x] 11.5 Only after the Diagnostics canary passes 11.2–11.4 and the complete built-in-removal acceptance path remains green, publish Debug as immutable stable, non-draft, non-prerelease tag `v1.0.0` in `nilp0inter/debug-channel` with exactly one canonical `subspace-channel.zip` asset.
- [x] 11.6 Record Debug v1.0.0 provenance from GitHub and the exact uploaded bytes: owner ID `1224006`, resolved repository ID, release ID, asset ID, tag, publication timestamp, asset name/size, and SHA-256; verify the manifest repository ID equals the resolved durable ID and the archive contains only the expected source entries.
- [x] 11.7 Re-resolve and download Debug anonymously through the production GitHub path, verify Official classification, exact digest/size/identity, strict validation, provider registration without execution, and generic instance creation; reject coordinate, label, tag, or mutable branch data as identity/provenance substitutes.
- [x] 11.8 Pin the exact public release bytes/digests and provenance in test/evidence inputs needed for repeatable acceptance while keeping all external package bytes and source out of production APK resources, automatic installation, ordinary startup, and Debug-specific GitHub/client branches.

## 12. Production UI, Physical Routing, and Release-Acceptance Evidence

- [ ] 12.1 On the supported physical device, enter the real public Diagnostics and Debug repository URLs, anonymously resolve their Official owner/repository/release/asset identities, inspect and install the exact canonical assets through the production package-management UI, and create instances only through the generic catalogue provider picker.
- [ ] 12.2 Exercise the public Diagnostics lifecycle on device: explicit update from an old rejected installation to v1.2.0, startup/ready and unselected heartbeat, metadata-only PTT plus audio reclamation, SOS/log projection, rollback where a retained valid revision exists, removal with unavailable definition, reinstall, and restart with fresh Lua state.
- [ ] 12.3 Create two external Debug instances with distinct modes, verify generic choice rendering and isolated persisted snapshots, edit one mode through the host UI, and record predecessor close/revocation before successor readiness with the sibling and catalogue order/active ID unchanged.
- [ ] 12.4 In Work mode, exercise all five Debug modes through PTT; record the four playback-producing modes’ deferred queue acceptance and exactly-once target-RSM Bluetooth SCO playback with voice-communication usage, the STT no-playback result, the 5.0-second delayed FIFO behavior, terminal status ordering, and no local/cross-channel/stale fallback.
- [ ] 12.5 In On-a-pinch mode, exercise all five Debug modes; record built-in-speaker playback in normal audio mode with media usage for the four playback-producing modes, STT without playback, queue/capability terminals, audible results, and absence of SCO/car/ambient fallback or duplicate output.
- [ ] 12.6 In On-the-road mode, exercise all five Debug modes; record Telecom capture release before each playback admission, validated car-media endpoint/device type and media usage for the four playback-producing modes, STT without playback, audible results, and absence of released-PTT-route reuse, phone-local fallback, duplicates, or cross-channel output.
- [ ] 12.7 On device, prove deferred ownership races: two delayed entries remain FIFO, a temporary selection change retains them without playing through the newly selected channel and allows admission after Debug is reselected, while mode edit/disable/package removal/generation replacement deletes predecessor entries and suppresses every late playback/log/status effect.
- [ ] 12.8 Record each physical case with selected instance ID, repository-derived provider ID, artifact digest, runtime generation, configured mode, capture terminal ordering, capability start/terminal outcomes, queue acceptance, route admission, endpoint/device type/usage, audible result, and explicit absence of ambient fallback, duplicate playback, stale predecessor effect, and cross-channel playback.
- [x] 12.9 Build and inspect debug and release-equivalent APKs to prove the existing single Lua JNI library and generic installer/runtime are present while external Debug/Diagnostics package bytes, built-in Debug code, test fixtures/providers, credentials, package signing/attestation additions, loose/native plugin loaders, and new Android permissions/SDK or foreground-service-retention changes are absent.
- [x] 12.10 Verify the final public surface and production composition contain no API v2, compatibility range/negotiation, legacy parser/callback/input shim, built-in Debug fallback or auto-migration, HTTP/JSON/filesystem/socket/database/event-loop/persistent-plugin-state API, dynamic host-resource/secret configuration, durable channel message or RSM custom-menu API, automatic update/discovery/App-Link flow, or Journal/Keyboard/OpenAI migration; also prove the remaining built-ins operate without Lua.
- [ ] 12.11 Re-run strict OpenSpec validation for `migrate-debug-channel-to-lua` after every automated, publication, provenance, APK, and device evidence item above is linked to its exact task; gate evolved-app publication on immutable Diagnostics v1.2.0 and Debug v1.0.0 availability plus complete evidence, with rollback defined as reverting the whole app change rather than mutating published assets or restoring a migrated-version fallback.

## 13. Device-Discovered Defect Fixes

- [x] 13.1 Fix the Debug ECHO/DELAYED_ECHO "1 pending message" stall: an actor continuation resumed synchronously from inside the serialized `INPUT_RELEASED` callback (the inline `playback.schedule` resume path) must not re-acquire the non-reentrant per-generation execution lock held by that callback. Tag serialized request executions with a coroutine-context marker and make `RuntimeGenerationInvocationGate.invokeContinuation` skip the lock when that marker is present while preserving liveness/closed checks, so the PTT terminal sequence, host-audio capture release, and deferred playback pump complete promptly instead of waiting out the input-release deadline.
- [x] 13.2 Add a deterministic `RuntimeInvocationBoundaryTest` regression proving a continuation resumed from inside a committed `INPUT_RELEASED` callback completes without timeout or generation invalidation and leaves the generation live for later callbacks.
- [x] 13.3 Update the `channel-runtime-invocation` spec delta with the continuation-resume re-entrancy invariant and a matching scenario.
- [x] 13.4 Install the corrected build on the supported device (On-a-pinch mode) and verify Debug ECHO plays back immediately after PTT release with no lingering pending-response pill and with subsequent PTT captures working.
- [x] 13.5 Fix the TTS and STT_TTS chained-yield stall: after `resumeInput` resumes a `handle_input` that yields another semantic operation (synthesize → playback.schedule for TTS; transcribe → synthesize → playback.schedule for STT_TTS), dispatch each successive chained yield under the same input invocation owner until the callback terminates, instead of awaiting the first operation's terminal forever.
- [x] 13.6 Mint a fresh `SemanticAudioOperation` terminal gate for each chained yield continuation in `resumeInput`, so a later executor (e.g. `executePlayback` after `executeSynthesis`) can claim its own gate instead of being rejected by the already-claimed predecessor gate.
- [x] 13.7 Install the corrected build on the supported device (On-a-pinch mode) and verify Debug TTS and STT_TTS produce audible synthesis after PTT capture, with no lingering pending state and with subsequent PTT captures working.
- [x] 13.8 Update the `lua-audio-api` spec delta with the chained-yield dispatch invariant and a matching scenario under the exactly-once resolution requirement.
- [x] 13.9 Restore the pre-migration Work playback route authority model: the target-RSM SCO subsystem proof (target-specific voice-recognition start, target HFP audio connection polling, and transport selection under that proof) is the authoritative ownership signal; remove the OEM-fragile endpoint-metadata cross-check (`isOwnedTargetSco`) that vetoed proven transports on devices whose framework labels the SCO communication endpoint with the phone's own identity. Fixes Work ECHO, DELAYED_ECHO, RSM Control-mode announcements, and every other `strategyFor(Work)` host playback path on such devices.
- [x] 13.10 Stop phone (On-a-pinch) playback acquisition from treating the app's own Work SCO lease or keep-warm residue as a busy foreign route (`acquirePhone` now consults `workSco.isActive()`; only routes the app does not own keep acquisition busy). Fixes delayed entries recorded in Work that could not drain through the speaker after a mode switch to On-a-pinch while the SCO transport stayed warm.
- [x] 13.11 Update `PlaybackRouteStrategyTest`: subsystem-proven transport acceptance regardless of endpoint metadata, missing-transport rejection with release, phone acquisition under owned Work SCO state resolves the built-in speaker, and phone acquisition stays busy under a foreign communication route.
- [x] 13.12 Update the `debug-channel` spec delta with the `On-a-pinch admission under owned Work SCO residue` scenario under the host-routed deferred playback requirement.
- [x] 13.13 Device verification: Work ECHO/DELAYED_ECHO via RSM, delayed-echo drain on the speaker after switching Work to On-a-pinch mid-delay, the inverse switch, and RSM Control-mode channel announcements.
- [x] 13.14 Restore On-the-road (car) response playback: `acquireCar` releases any stale Work (RSM) SCO lease/keep-warm (which holds `MODE_IN_COMMUNICATION` + SCO and blocked car admission), then plays through the car media path — a validated car media output (`AUX_LINE`/`BLUETOOTH_A2DP`/`BUS`/`HDMI`/`USB_DEVICE`/`USB_HEADSET`) when one is enumerable, otherwise unpinned `USAGE_MEDIA` default media routing to the A2DP sink — instead of failing closed when no device is enumerable (this OEM exposes no routable car output device to `getDevices()`, which stranded every response). Mirrors the proven pre-host-owned `MediaResponsePlayer` path.
- [x] 13.15 Car playback requests transient `USAGE_MEDIA` audio focus best-effort via `CarMediaFocus` + `MediaFocusPlaybackRoute`: focus is requested before playback and abandoned on release, but denial does not block playback (the on-the-road spec requires requesting focus AND playing; a head unit still renders the A2DP stream when transient focus is unavailable).
- [x] 13.16 Extend `PlaybackRouteStrategyTest`: A2DP car media output is accepted and pinned; missing enumerable car media falls back to unpinned `USAGE_MEDIA`; a stale Work route is released before car acquisition; focus is requested before playback and abandoned on release; focus denial plays through best-effort. Suite green (30 tests).
- [ ] 13.17 Device verification (car / Android Auto): capture via the HFP self-call, hang up, and confirm queued channel responses play over A2DP with no redial. *(Requires the physical car head unit; phone currently unavailable.)*
