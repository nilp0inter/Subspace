## 1. Baseline Ownership and Callsite Characterization

- [x] 1.1 Record every `LuaProofBridge` callsite (`LuaProofBridgeFactory`, `LuaProofNativeBridge`, `LuaProofHandles`, `LuaProofOutcome`, `LuaProofOutcomeCodec`, `LuaProofNative`, `LuaIdentifiers`, `LuaBridgeTopology`) and the proof instrumentation harness entrypoint (`LuaProofInstrumentationTest.kt`) before any rename, identifying which paths are proof-only versus retained kernel contracts
- [x] 1.2 Record the current `ChannelRuntimeRegistry` replacement, committed-input-lease, retirement, shutdown, and `RuntimeGeneration` assignment flow and the existing `RuntimeInvocationBoundary`/`RuntimeGenerationInvocationGate` admission, serialization, cancellation, timeout, committed-target, `stopAdmission`, `invalidate`, `commitIfLive`, and idempotent-close surface as the integration baseline
- [x] 1.3 Record the current `RevocableChannelCapabilityScope`/`ChannelCapabilityHost` lease, revocation, descriptor-eligibility, `CapabilityScopeIdentity(channelInstanceId, runtimeGeneration)`, and `Closed`/`Cancelled` late-effect behavior as the capability integration baseline
- [x] 1.4 Record the current `ChannelRuntime` contract (`prepareInput`, `handleSos`, `refreshReadiness`, idempotent close, `ChannelInputAcceptance`, `StateFlow<ChannelRuntimeSnapshot>`) and the existing Kotlin provider registration/construction paths (`BuiltInRuntimesTest.kt`, `ServiceIntegrationTest.kt`, `ServiceAgentRuntimeComposition.kt`) as the Kotlin-compatibility baseline
- [x] 1.5 Record the existing Rust conformance suite (`rust/subspace-lua-proof/subspace-lua-proof/tests/conformance.rs`), JNI entrypoints, and outcome codec as the native contract baseline and identify which assertions must be retained verbatim versus extended for actor scope
- [x] 1.6 Run the focused existing baseline tests — Rust conformance, JVM `RuntimeInvocationBoundaryTest.kt`, `ChannelRuntimeRegistryTest.kt`, `RevocableChannelCapabilityScopeTest.kt`, `LuaProofHandleContractTest.kt`, `LuaProofOutcomeCodecTest.kt`, `LuaProofBridgeFactoryTest.kt`, and `BuiltInRuntimesTest.kt` — and record their pre-change results as the migration oracle

## 2. Proof-Kernel Clean Promotion and Native Contract Preservation

- [x] 2.1 Rename the `jvm_owned` kernel types from `LuaProof*` to production-internal actor-bridge names under the existing `dev.nilp0inter.subspace.lua` package, retaining the eight JNI entrypoints (`create`/`load`/`start`/`resume`/`cancel`/`interrupt`/`snapshot`/`close`), opaque handle model, normalized outcome sealed type, per-state serialization lock, instruction hook, and per-state allocator
- [x] 2.2 Remove the proof-only `LuaBridgeTopology` switch, `native_owned` candidate path, feature flags, and any dormant alternative implementation as part of the rename cutover so exactly one internal kernel remains
- [x] 2.3 Retain the Rust crate workspace/manifest, `mlua` 0.12.0 binding, Lua 5.4.8 source-only build, JNI panic boundary, and the `conformance.rs` suite under the promoted internal crate name; remove candidate-only JNI exports and proof-only test paths rather than duplicating them
- [x] 2.4 Migrate the retained Rust conformance suite to the promoted kernel name and extend it: two states with equal global/module names observe only their own values; closing one leaves the other resumable and unchanged; malformed source, binary bytecode, missing entrypoint, foreign handle, stale handle, and post-close completion return a normalized typed outcome without executing a later Lua effect
- [x] 2.5 Add Rust conformance cases that a protected ordinary Lua error is normalized while a separate state remains usable, and that no assertion claims containment for native panic, corruption, process OOM, or process death; all resource-boundary tests use explicit non-default memory/instruction inputs
- [x] 2.6 Add Rust kernel terminal-admission and single-entry conformance cases at the JNI/kernel boundary: success, normalized failure, cancellation, duplicate terminal delivery, and completion-versus-cancellation/close races produce one observable coroutine effect; every losing terminal request reports the accepted terminal state or a typed stale/closed result
- [x] 2.7 Add Rust single-entry conformance cases: two simultaneous starts/resumes on one state yield at most one entered Lua callback; distinct states make independent progress; a yielded operation returns before external completion and interrupting or closing it prevents its later Lua/native effect while leaving a healthy peer usable
- [x] 2.8 Preserve source-only and containment Rust conformance cases as actor-facing native boundary regressions: binary chunks and `package.loadlib`/C-searcher/shared-library loading are rejected before executing the supplied payload and the owning actor remains usable; pure-Lua infinite-loop/recursive/metamethod workloads are interrupted within a finite instruction-and-elapsed bound, are closable, and do not stop an unrelated state
- [x] 2.9 Add Rust allocator-denial conformance cases at explicit limits: denial during construction/load/coroutine creation/resume normalizes to memory failure, records a denied allocation, remains closable, and does not alter a peer state's accounting or usability; terminal close reports zero remaining Lua-owned bytes while retaining peak and separately attributing bridge bytes
- [x] 2.10 Run the migrated and extended Rust conformance suite and confirm every retained proof assertion still passes under the promoted name with no proof-only path remaining

## 3. Opaque Actor Identities and Outcomes

- [x] 3.1 Extend the kernel's `LuaStateHandle(stateId, generation)`, `LuaCoroutineHandle`, and `LuaOperationHandle` model to actor scope as opaque generation-safe identifiers; ensure no native pointer, Lua registry index, or raw Kotlin object crosses the Lua boundary
- [x] 3.2 Add actor event, task, and operation identities as language-neutral host-domain values carrying channel instance ID, runtime generation, task/event identity, and operation identity; durable channel operations additionally carry durable-run identity while ordinary timers, startup work, and background operations do not receive fabricated durable-run identities
- [x] 3.3 Layer actor identity on the existing `CapabilityScopeIdentity` and registry generation contracts rather than creating a competing generation space; keep authorizations opaque host-domain values
- [x] 3.4 Add JVM tests that a completion, cancellation, or close request bearing a stale, foreign, or closed identifier returns a typed terminal outcome (`stale`, `invalid-owner`, `closed`) without entering Lua or producing another native effect, and that the current generation's Lua state is not mutated
- [x] 3.5 Deliver mailbox events as opaque envelopes carrying event kind, owning generation, and host-domain payload; decode envelopes internally so the host does not expose Kotlin classes, Android callbacks, Compose state, SDK types, or transport objects in the envelope

## 4. Bounded Mailbox and Cooperative Ready/Suspended Scheduler

- [x] 4.1 Implement one bounded event mailbox per actor that admits events in deterministic FIFO order and rejects new events with a typed `Busy` result when no capacity remains, without allocating an unbounded waiter queue or starting the rejected event later
- [x] 4.2 Implement the cooperative ready-coroutine scheduler: a ready queue holds eligible coroutines, a suspended set holds coroutines waiting on host-operation tokens, the scheduler runs in the generation-owned child scope on the bounded runtime worker dispatcher, and at most one Lua coroutine executes at a time
- [x] 4.3 Ensure every dequeue validates the generation's live state and every Lua entry uses the kernel's per-state serialization lock; a continuation does not occupy the host invocation FIFO merely because it belongs to the same generation
- [x] 4.4 Make a coroutine ready when the actor entry starts it, a host operation completes, a timer fires, or a mailbox event is delivered; make it suspended when it yields an internal operation token and receives an opaque token; the scheduler does not preempt and the instruction hook interrupts only pure-Lua loops, not suspended coroutines
- [x] 4.5 Add JVM tests with an explicit small mailbox capacity: concurrently admitted events start their first Lua slices FIFO without overlap; overflow returns typed `Busy` and its callback never later executes
- [x] 4.6 Add JVM tests that after event A yields an opaque operation the host adapter returns normalized yielded/admitted immediately and event B can enter; A's continuation is resumed from the actor ready queue, not re-admitted as a host callback
- [x] 4.7 Add JVM tests that multiple yielded coroutines suspend independently; a completion arriving while another coroutine is entered queues its owner and enters it only after the running slice yields/completes, proving one Lua entry at a time via a recording Lua script/fake that appends semantic event/effect labels
- [x] 4.8 Add JVM tests that events for different live actors progress while one actor is held, proving no cross-instance mailbox/lock serialization; use `StandardTestDispatcher`, `runCurrent`, `CompletableDeferred` slice/operation gates, and no wall-clock waiting

## 5. Generation-Owned Task/Operation Scopes and Exactly-Once Terminal Races

- [x] 5.1 Implement a background-task scope bounded by the generation's lifetime and invocation gate, tracking every actor task so that generation retirement cancels or drains all descendants and no work escapes the generation to become process-global
- [x] 5.2 Implement operation-token ownership: each suspended operation token is generation-safe and accepts at most one terminal completion, cancellation, or close; a resume/cancel/close for a foreign or stale token returns a typed outcome without entering Lua; the scheduler resumes at most one coroutine per ready dequeue and at most one terminal completion per token
- [x] 5.3 Add JVM tests that an active pure-Lua slice exceeding an explicitly small budget is interrupted and the actor becomes closable; yielded wait time does not consume the active execution budget (use `advanceTimeBy` virtual time)
- [x] 5.4 Add JVM tests that a yielded operation exceeding its explicit wait deadline resumes its owner exactly once with typed timeout; cancellation versus completion allows exactly one terminal winner and never produces two owner effects
- [x] 5.5 Add JVM tests that caller cancellation removes queued work before its slice begins; cancelling an entered slice reaches its child operation; generation cancellation invalidates suspended tokens without resuming them to publish effects
- [x] 5.6 Add JVM tests that close atomically refuses further admission, invalidates queued work, cancels descendants, revokes capability authorization, invokes terminal Lua close at most once, completes within the explicit close bound even if a callback ignores cancellation, and rejects every later completion as closed/stale; repeated close observes the same terminal closed state
- [x] 5.7 Add JVM tests using `Barrier`-released threads (not sleeps) for true terminal races: duplicate completion, completion-after-return, completion-versus-cancellation, and close-versus-completion produce exactly one winner and the loser observes a typed terminal result without a second Lua effect or freed-memory access

## 6. Host Callback/Continuation Integration

- [x] 6.1 Implement the host-callback path: the host invokes the actor through the generation's invocation gate to deliver an event; the callback is serialized and bounded while it admits the event and runs that event's current Lua slice; if the slice yields, the callback returns a normalized admitted/yielded result and releases its gate slot while the suspended coroutine remains actor-owned
- [x] 6.2 Implement the actor-continuation path: a suspended coroutine becomes ready when the host completes, cancels, or fails its operation token; the scheduler resumes it under the generation's live-state gate and per-state serialization lock, not as a new host invocation-queue callback
- [x] 6.3 Ensure a yielded event does not hold the serialized host-callback slot while external work is pending and later host events may be admitted while its coroutine remains suspended; a continuation carries the operation token's generation, task, and operation authorization
- [x] 6.4 Ensure the actor does not expose raw Kotlin coroutine jobs, Android callbacks, or SDK futures to Lua; both host callbacks and continuations cross the opaque bridge boundary as normalized outcomes and opaque handles
- [x] 6.5 Extend `RuntimeInvocationBoundaryTest.kt` with actor-aware cases: the invocation gate serializes only adapter admission/current Lua slice; yield releases the gate slot allowing a later host callback; an operation completion does not masquerade as a new host-admitted callback and cannot overlap native Lua entry
- [x] 6.6 Extend `RuntimeInvocationBoundaryTest.kt` with mailbox-saturation mapping through the runtime boundary to typed busy/unavailable with no retained overflow waiter and no delayed execution of the rejected event
- [x] 6.7 Extend `RuntimeInvocationBoundaryTest.kt` that a callback timeout/cancellation remains distinguishable from ordinary runtime failure, invalidates late commits, and does not poison unrelated generations; reuse the registry fixture's `RuntimeWorkerDispatcher.fromDispatcher(StandardTestDispatcher(testScheduler))`, `TestRuntime`/`RecordingTarget` pattern, and a recording actor runtime fake

## 7. Startup, Readiness, and Local-versus-Fatal Failure Policy

- [x] 7.1 Implement the actor lifecycle `constructing → staged → starting → live(ready) → retiring → drained → closed` with the `failed` failure-latch branch, bounded by the generation's invocation gate and the registry's entry lifecycle
- [x] 7.2 Ensure before readiness the actor may create state, load source, validate entrypoint, and execute the bounded protected startup entry authorized by the host, but SHALL NOT admit ordinary mailbox events or start plugin background work; a not-ready actor refuses ordinary events with a typed not-ready result and does not queue them
- [x] 7.3 Ensure the host publishes readiness only after protected startup reports a normalized ready outcome; a startup coroutine suspended on an operation remains initializing until it resumes and reports ready
- [x] 7.4 Implement the per-actor failure latch: a protected ordinary event or background-task error is normalized and contained to that event/task; a failed host operation resumes its owning coroutine with a normalized failure and does not by itself latch the actor
- [x] 7.5 Ensure startup, lifecycle-critical, instruction, memory, ownership-integrity, or another generation-fatal outcome latches the actor as failed, prevents further Lua entry, projects the actor as unavailable, and closes the failed generation deterministically without terminating unrelated actors
- [x] 7.6 Ensure the runtime makes no containment claim for native engine/bridge defects, unprotected panic, native memory corruption, unrecoverable process OOM, or Android process death; the actor treats unrecognized native defects as generation-fatal and relies on the host to retire and restart
- [x] 7.7 Ensure the host owns the failure latch and decides when to retire a failed generation and construct a successor; the actor never resets its Lua state silently within the same generation
- [x] 7.8 Add JVM synthetic-operation tests using the future test-only actor harness and `runTest`/`StandardTestDispatcher(testScheduler)`: initial startup stays initializing while its startup coroutine is suspended; ordinary event and background-work admission return typed not-ready and are never queued; completing startup publishes ready and then admits work
- [x] 7.9 Add JVM tests that ordinary protected event/background-task errors and normalized host-operation failures are delivered only to their owner and leave the actor ready; startup/lifecycle-critical/instruction/memory/ownership failures latch failed, prohibit later Lua entry, and do not make a peer actor unavailable
- [x] 7.10 Add JVM tests that the readiness/failure latch is host-projected through `ChannelRuntimeSnapshot`, is not cleared by later actor activity, and is fresh for a reconstructed generation; do not assert class construction or latch field assignments

## 8. Configurable Instruction/Memory/Mailbox/Deadline Policies

- [x] 8.1 Implement a host-owned internal configuration supplied at actor construction (not plugin configuration) covering: per-state Lua allocator limit (starting 4–8 MiB), instruction hook interval (starting 100–1000), operation-specific instruction budgets, mailbox capacity, callback deadlines and close timeout, and background-task scope bounds (max concurrent tasks, per-task deadline)
- [x] 8.2 Implement the instruction-count hook (or equivalent protected check) that interrupts pure-Lua execution exceeding a configured instruction budget within a finite instruction-and-elapsed bound, normalizes the actor as failed/closing, permits deterministic teardown, and leaves unrelated actors usable; the hook is not claimed to preempt blocking native host functions
- [x] 8.3 Implement the per-actor Lua allocator that records current and peak Lua-managed native bytes and denies allocation at a configured limit; denial under protected execution returns a normalized memory failure where Lua permits recovery, leaves the actor closable, and does not alter another actor's accounting
- [x] 8.4 Implement the active-execution-budget versus yielded-wait-deadline distinction: the active budget bounds pure-Lua computation while the VM is entered and is not charged for yielded wait time; a yielded operation exceeding its configured wait deadline is completed with a typed timed-out result and the owning coroutine is resumed exactly once with that normalized result
- [x] 8.5 Implement internal diagnostics feeding host-owned observability for current/peak memory, denial counts, instruction counts, and latency; policy values are evidence-derived, not persisted as plugin-facing limits, and do not appear in the public configuration schema
- [x] 8.6 Add JVM tests that policy values are configurable stimuli, not asserted defaults: inject explicit memory limit, instruction budget, hook interval, mailbox capacity, callback deadline, and close timeout and assert semantic timeout/busy/interruption/memory outcomes; do not assert a production policy constant as a compatibility contract

## 9. Registry Staged Successor and Drain-Before-Ready Cutover

- [x] 9.1 Strengthen `ChannelRuntimeRegistry` replacement sequencing so generation H may create its state and load/statically validate source while G drains, but H does not execute plugin startup, acquire effect authorization, or publish readiness until G has stopped admission, completed any committed input terminal callback, cancelled or drained descendants, revoked effects, and closed exactly once
- [x] 9.2 Ensure the registry retains H as a staged pending generation while G is live; once G's `closed` deferred completes, the host issues H's generation authorization, executes H's bounded protected startup, and publishes H ready only if startup succeeds; no plugin effect from H overlaps a live G
- [x] 9.3 Ensure ordinary replacement does not treat a timeout as permission to expose two live generations; shutdown remains bounded by the existing shutdown policy
- [x] 9.4 Ensure the successor H acquires a fresh Lua state and does not inherit G's suspended coroutines, volatile tasks, or prior-generation authorization; durable host records remain available to H through explicit host recovery
- [x] 9.5 Ensure the predecessor's committed terminal callback is not delivered to the successor and new preparation is routed to the successor only after the predecessor has closed
- [x] 9.6 Extend `ChannelRuntimeRegistryTest.kt` with drain-before-ready replacement cases using `CompletableDeferred` to hold the predecessor terminal callback and descendants: G stops preparation admission but its committed target receives exactly one terminal release/cancellation/failure callback before its lease is released; G's background tasks/tokens are cancelled or drained, effects revoked, and G closes once before H starts or becomes ready
- [x] 9.7 Extend `ChannelRuntimeRegistryTest.kt` that a staged H cannot receive event/effect authorization while G is live; after G closes H starts from a fresh state and can become ready; G's late completion/capability attempt is stale/closed and cannot mutate H's snapshot, publish text/audio, execute a tool, or advance current state
- [x] 9.8 Extend `ChannelRuntimeRegistryTest.kt` with fresh-generation recovery cases: after a simulated service/process restart, durable host records may be supplied to an explicit fresh recovery operation, but old mailbox entries, coroutines, operation handles, ready positions, readiness/failure latches, and old-generation completions cannot affect the new generation; assert recorded terminal/effect order, not implementation calls

## 10. Capability/Effect Revocation and Service Shutdown

- [x] 10.1 Implement actor-to-host capability mediation: when Lua needs a host effect it yields an opaque operation token; the host resolves it through the existing `RevocableChannelCapabilityScope`; the completion is delivered back as an actor continuation; the actor does not reimplement, wrap, or bypass host capabilities
- [x] 10.2 Ensure any blocking runtime or host operation yields before execution; synchronous native host functions are permitted only for deliberately tiny bounded fixed-size work; filesystem, network, model, Android, or other external work always yields an opaque operation token before execution and the Lua entry thread is never blocked
- [x] 10.3 Ensure generation-bound capability leases are revoked before the predecessor closes during replacement; a late completion bearing G's generation is classified as stale without entering H's Lua state; use-after-revocation returns `Closed`/`Cancelled` without effect
- [x] 10.4 Implement service-shutdown participation: stop mailbox admission immediately when the gate stops admission; let any executing host callback reach its terminal phase or timeout within the gate's close bound; cancel suspended operation tokens and join background tasks within `closeTimeoutMillis`; close the Lua state idempotently through the kernel's `close` operation invalidating all descendant handles; produce no late effects after close
- [x] 10.5 Ensure durable host records survive shutdown while Lua states, suspended coroutines, volatile tasks, and prior-generation authorization do not; on restart the registry assigns fresh generations and recovery of persisted durable runs is an explicit host operation under the fresh generation that does not import prior volatile Lua context
- [x] 10.6 Ensure the actor does not own foreground-service retention; shutdown is host-initiated and host-bounded; the actor does not delay it beyond the configured bound
- [x] 10.7 Extend `ChannelRuntimeRegistryTest.kt` with shutdown cases: service shutdown prevents new preparation, requests terminal PTT handling, waits only for the bounded committed-terminal phase, closes all live/retired actors once, and remains idempotent
- [x] 10.8 Extend `RevocableChannelCapabilityScopeTest.kt` and registry integration fakes: an actor operation yields an opaque host operation; the recording semantic capability receives only host-domain request/authorization data and returns a normalized completion/failure; no test runtime is given an Android context, transport, socket, recorder, file descriptor, SDK client, or raw device object
- [x] 10.9 Extend capability-boundary regression coverage: revoking predecessor authorization before close makes a late capability lease operation return closed/cancelled with zero semantic effect; the replacement generation can acquire and use its own capability port
- [x] 10.10 Extend capability-boundary regression coverage: a local host-operation failure resumes only the owning coroutine and does not latch actor failure or stop an unrelated actor; policy-level retry/poll decisions remain observable only as actor-selected subsequent operations while host cancellation still wins retirement; use the existing hand-written `RecordingCapabilityHost`/recording ports and assert the absence of a recorded effect after revocation

## 11. Synthetic JVM/Rust/Instrumentation Conformance

- [x] 11.1 Implement test-only synthetic operations covering actor construct → ready → failed → close transitions, mailbox admission/bounded-rejection/ordering, coroutine yield/resume/cancel races under the generation gate, failure latch set and projection as unavailable, replacement G-drain → H-ready publication ordering, shutdown stop-admission → drain → close within bound, generation-safe late-effect suppression, and instruction-hook/allocator-denial containment inside the actor
- [x] 11.2 Ensure synthetic operations are internal test utilities: not exposed as a public API, not registered as a provider, and not wired into application startup
- [x] 11.3 Add JVM tests that a non-active enabled actor can complete independent background work while a contemporaneous PTT input reaches the selected active runtime only; changing selection does not cancel a third actor's generation-scoped work (reuse the registry fixture's recording actor runtime fake exposing external input/effect events)
- [x] 11.4 Repurpose `LuaProofInstrumentationTest.kt` into internal actor-runtime instrumentation preserving its status-channel evidence protocol; remove the proof-only user surface as part of this cutover so no proof-only entrypoint remains
- [x] 11.5 Implement the device instrumentation workload covering actor startup/readiness, bounded mailbox rejection, yield-slot release, multiple suspended coroutines, instruction interruption, allocator denial, fatal-versus-local containment, replacement late-effect suppression, and bounded shutdown with synthetic source/operations
- [x] 11.6 Ensure each device correctness probe is a pass/fail assertion and the emitted status JSON records device/API/ABI/build/native provenance, actor policy parameters, probe outcomes, normalized outcomes, timing/allocation observations, and race outcomes; recorded values remain observations rather than acceptance thresholds
- [x] 11.7 Ensure JVM and Rust synthetic tests use deterministic coordination (`StandardTestDispatcher`, `runCurrent`, `CompletableDeferred` gates, `Barrier`-released threads, `advanceTimeBy` virtual time) and never wall-clock sleeps for races, scheduling, or device waiting

## 12. Kotlin-Provider and No-Startup-Lua Regression Barriers

- [x] 12.1 Ensure the change does not register a Lua channel provider; ordinary application startup creates no Lua actor or Lua state for any channel instance; existing Kotlin providers remain behaviorally supported and do not acquire a Lua runtime
- [x] 12.2 Ensure a Lua actor is created only when a future package/API change supplies a Lua-backed provider; the runtime does not alter persisted channel definitions, provider registration, channel routing, PTT, audio, UI, foreground-service, or release behavior
- [x] 12.3 Ensure existing capability contracts remain usable by built-in Kotlin runtimes without constructing a Lua actor or Lua state; no public runtime I/O module or persistent plugin-state capability is present
- [x] 12.4 Add a host composition regression using the existing Kotlin catalogue, reconciliation, and a representative Kotlin-provider PTT/capability operation that succeeds with zero actor-state creation/native-load attempts recorded by an internal test-only kernel/actor factory diagnostic
- [x] 12.5 Add a regression covering ordinary service restart/reconciliation: existing Kotlin providers retain current functional behavior and no Lua actor/state is introduced by readiness refresh, selection change, background service activity, or shutdown
- [x] 12.6 Add a production Android lifecycle witness through `MainActivityServiceLifecycleTest.kt`/`PttForegroundService`: normal activity/service startup and foreground lifecycle complete while the internal actor creation diagnostic remains zero; the diagnostic is observed alongside real startup behavior, not in a standalone factory-construction test
- [x] 12.7 Ensure no test asserts provider-list wiring, a private counter's stored value, or a default configuration; the defended contract is that real Kotlin channel operation and service lifecycle occur without native Lua allocation/loading until a future Lua-backed provider is genuinely registered (use existing `BuiltInRuntimesTest.kt` and `ServiceIntegrationTest.kt` behavioral fixtures)

## 13. Android Device Evidence and Affected Variant/Package Verification

- [x] 13.1 On a physical arm64 Android device, verify the production-packaged JNI bridge creates/closes states, executes actor yield/resume, rejects stale/foreign/closed completions, records exactly-once cancellation/close race outcomes, and proves state/actor isolation under concurrent callers
- [x] 13.2 Verify the packaged APK for the supported ABI contains the single promoted Lua native library required by the actor runtime and does not package the retired proof/candidate/alternative bridge libraries; the actor instrumentation workload links and executes against that packaged library on device
- [x] 13.3 Verify the promoted Rust crate is built from the intended standalone workspace/manifest with its required JNI panic boundary and the app package receives the library for the supported ABI; a missing or cross-compiled-wrong ABI fails packaging or device loading rather than silently omitting Lua support
- [x] 13.4 Verify the runtime rejects binary/native/module-loading attempts through its executed Lua boundary while a subsequent benign actor event succeeds; this is the behavioral no-public-I/O/no-public-FFI barrier (not a source-grep test for Gradle text, class names, imports, comments, or feature flags)
- [x] 13.5 Run the dedicated actor instrumentation class on the physical arm64 target and archive the full status-channel evidence JSON with the evaluated APK/build provenance
- [x] 13.6 Perform release-equivalent APK artifact verification for the supported ABI before accepting the device evidence (inspect built APK contents and run the physical-device actor instrumentation)
- [x] 13.7 Confirm each race/saturation/timeout test has a plausible mutation that reddens it: admitting overflow later, resuming twice, failing to release the gate on yield, publishing H before G close, allowing a stale effect, or treating a local failure as fatal